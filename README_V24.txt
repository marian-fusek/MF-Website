V24 fixes:
- Clean hero name scaling: measures actual rendered text range and scales whole wordmark between nav-aligned left margin and right margin + 40px K compensation.
- Text selection highlight is white with black text.
- Grid hover now allows multiple touched line segments to fade independently during fast mouse movement.
- Grid hover glow widened while keeping line thickness at 1px.
Updated files only: script.js, style.css.
